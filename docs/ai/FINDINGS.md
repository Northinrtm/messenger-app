## Findings

- 2026-05-06: `docs/ai/*` project-memory files were missing and had to be bootstrapped.
- 2026-05-06: worktree already contains an active migration from `EncryptedMessagePayload*` to `PlainMessagePayload*`.
- 2026-05-06: a hidden backend dependency on the old message schema remained in `MessageReceiptRepository` unread queries (`message.encryptionScheme`); removing it was required for full `mvn test` coverage.
- 2026-05-06: verification succeeded with `backend: mvn -q test` and `web: npm run typecheck`, `npm run test:run`, `npm run build`.
- 2026-05-07: `deploy/bootstrap-prod-env.sh` and `deploy/preflight-prod.sh` needed explicit MinIO defaults/validation after compose switched attachment storage from local volume to MinIO.
- 2026-05-07: verification succeeded after attachment/storage refactor with `backend: mvn -q test`, `web: npm run typecheck`, `web: npm run test:run`, `web: npm run build`, and `bash deploy/preflight-prod.sh .env.prod.example`.
- 2026-05-07: direct MinIO transfer also required a public endpoint (`APP_MEDIA_MESSAGE_ATTACHMENTS_MINIO_PUBLIC_ENDPOINT`) plus edge/dev proxying on `/storage`; signing internal `http://minio:9000` URLs is not browser-usable.
- 2026-05-07: after direct MinIO transfer stabilized, the old multipart proxy endpoints (`POST /api/chats/{chatId}/attachments`, `GET /api/chats/{chatId}/attachments/{id}`) and filesystem storage backend were dead compatibility code and could be removed safely.
- 2026-05-07: message-at-rest encryption touched more than `sendMessage`; preview rendering, reply snippets, websocket dispatch, and query hydration all needed a shared decrypt path because `ChatMessage` no longer carries plaintext from JPA.
- 2026-05-07: verification succeeded after encrypted message persistence with `backend: mvn -q test`, `web: npm run typecheck`, `web: npm run test:run`, `web: npm run build`, and `bash deploy/preflight-prod.sh .env.prod.example`.
- 2026-05-07: after fresh-start-only policy was confirmed, Flyway migrations were squashed back to a single `V1__baseline.sql`; backend tests and production preflight still passed on the flattened schema.
- 2026-05-07: verification also stayed green after removing attachment proxy/filesystem compatibility paths: `backend: mvn -q test`, `web: npm run typecheck`, `web: npm run test:run`, `web: npm run build`, `bash deploy/preflight-prod.sh .env.prod.example`.
- 2026-05-07: `deploy/remote-update.sh` originally cleaned all untracked files and could delete server-owned `.env.prod`; the script now excludes `ENV_FILE` from dirtiness checks and `git clean` so bootstrap/preflight do not silently rotate or lose production secrets.
- 2026-05-07: GitHub deploy workflow logic was ported into a local Windows operator trigger (`deploy-prod-button.ps1`), which uploads `remote-update.sh`, tails remote logs, and verifies public `build-meta.json` without GitHub Actions.
- 2026-05-07: manual deploy trigger verification covered `powershell [scriptblock]::Create(...)` parsing for `deploy/deploy-prod-button.ps1`, `bash -n deploy/remote-update.sh`, `bash deploy/preflight-prod.sh .env.prod.example`, and `backend: mvn -q -Dtest=ProductionDeploymentConfigTest test`.
- 2026-05-07: local Docker/WSL startup exposed a real Postgres mapping bug: `chat_message_keys.encrypted_dek` was created as `bytea`, but JPA marked the field as `@Lob` and Hibernate expected `oid`. Fix was to remove `@Lob` and map encrypted byte arrays explicitly as `VARBINARY` in `ChatMessage` and `ChatMessageKey`.
- 2026-05-07: active-chat history hydration was clearing unread counts too early in the client because `useWorkspaceEffects` always pushed the last cached message through preview sync with unread mode `clear`; unread must stay server-driven until `isActiveChatOpen` becomes true.
- 2026-05-07: workspace navigation is expected to restore the current chat after refresh; premature read semantics must be solved in receipt/visibility logic, not by suppressing restored `activeChatId`.
- 2026-05-07: sender-side delivery UI was ambiguous because `DELIVERED` and `READ` were too close visually; current contract is `DELIVERED = one check` and `READ = two checks`, with labels still carrying receipt counts.
- 2026-05-07: fresh VPS deployment needed official Docker CE + Compose plugin/buildx on Ubuntu; older distro `docker.io/docker-compose` was not enough for the current image build path.
- 2026-05-07: the server IP hit Docker Hub anonymous rate limits for `jitsi/*` and `minio/minio`; preloading those images from the operator workstation and then running `deploy/remote-update.sh` kept the stack fully self-hosted on the VPS.
- 2026-05-07: production verification succeeded on `https://pishi.ktsf.ru/`, `https://pishi.ktsf.ru/meet/`, and `https://pishi.ktsf.ru/build-meta.json` after a clean `docker compose down --remove-orphans` and redeploy to revision `df8cff9`.
- 2026-05-07: on the 2 GB RAM + 2 GB swap VPS, the hardened prod stack stayed within limits after startup; sampled runtime usage showed backend about 427 MiB / 640 MiB and Jitsi JVB about 156 MiB / 192 MiB.
- 2026-05-07: a real production backend restart happened under normal auth flow with `java.lang.OutOfMemoryError: Metaspace`; the old `-XX:MaxMetaspaceSize=128m` was too low for the Spring app under live load.
- 2026-05-07: the production JVM hotfix was to raise backend metaspace to `192m` and lower heap cap from `MaxRAMPercentage=52` to `42`, then redeploy to revision `c1ccd42`; after rollout, backend came back with `RestartCount=0` and the new `JAVA_TOOL_OPTIONS` visible in logs.
- 2026-05-07: on the 2 GB VPS, the next safe production rebalance after the metaspace fix was `backend 768m`, `minio 192m`, `web 64m`; backend was the only core service consistently close to its limit, while web and MinIO had clear headroom.
- 2026-05-07: production SSH was moved off `root + password`; key login as `deploy` worked, `/opt/messenger-app` was chowned to `deploy`, and password-based root SSH was confirmed blocked after applying `/etc/ssh/sshd_config.d/99-messenger-hardening.conf`.
- 2026-05-07: the auth flow around `verification -> login` is smoother if login accepts either username or email; current implementation now keeps the existing `username` request field but resolves `email` identifiers server-side in `AuthService.login(...)`.
- 2026-05-07: targeted auth verification stayed green with `backend: mvn -q "-Dtest=AuthServiceTest,EmailVerificationServiceTest" test` and `web: npm run test:run -- AuthCard App`.
- 2026-05-07: GitHub button deploy can reuse the same SSH rollout path as the local Windows button by wrapping `deploy/remote-update.sh` in a small portable helper (`deploy/deploy-via-ssh.sh`) and feeding SSH key / known_hosts through GitHub secrets.
- 2026-05-07: GitHub SSH deploy hit a real server-side lock-file regression because earlier root-driven rollouts left `/tmp/messenger-remote-update.lock` owned by `root`; `deploy/remote-update.sh` now keeps its host-level lock under `$APP_DIR/.deploy/remote-update.lock`, which is writable by the `deploy` checkout owner.
- 2026-05-08: when the user reported the server as "down", the production stack was actually up: `backend/web/edge/postgres/redis/minio/jitsi*` all had `RestartCount=0`, public `/` and `/meet/` returned `200`, and `build-meta.json` served revision `b5706c8`.
- 2026-05-08: notable production anomalies during the server check were `backend` memory around `616 MiB / 768 MiB`, `jitsi-jicofo` near limit at `90.75 MiB / 96 MiB`, repeated websocket abnormal closes (`1001/1006`, `EOFException`) in backend logs, and Redis warning that `vm.overcommit_memory=1` is not enabled on the host.
- 2026-05-08: leftover exited anonymous helper containers (`exit 28`) from old MinIO download attempts were still present in `docker ps -a`; they are not part of the running stack but add noise during incident triage.
- 2026-05-08: `deploy` has no passwordless `sudo`, but because it is in the `docker` group, a temporary privileged container was sufficient to fix the host Redis prerequisite and persist `vm.overcommit_memory = 1` into `/etc/sysctl.d/99-messenger-redis.conf`.
- 2026-05-08: after the Redis host fix, the old exited helper containers were removed and the public site still answered `200`; the running production compose stack stayed healthy throughout the cleanup.
- 2026-05-08: the next production memory rebalance after the Redis cleanup is `jitsi-jicofo 128m`, `minio 160m`, `web 48m`; `jicofo` was the only Jitsi control-plane service critically close to its limit, while `web` and `minio` still had headroom.
- 2026-05-08: websocket `1001/1006` and `EOFException` disconnects are partly explainable by client lifecycle handling: `realtime.ts` already had hidden-tab pause/resume helpers, but they were not wired to `visibilitychange`, so background tabs relied on browser/network teardown instead of proactive pause/reconnect.
- 2026-05-08: the lifecycle websocket fix stayed green with `web: npm run test:run -- realtime`.
- 2026-05-08: production rollout to revision `648f561` succeeded through `deploy/remote-update.sh`; public `build-meta.json` updated, `vm.overcommit_memory` stayed at `1`, and sampled runtime usage after rollout was roughly backend `587 MiB / 768 MiB`, web `6 MiB / 48 MiB`, MinIO `73 MiB / 160 MiB`, Jitsi Jicofo `114 MiB / 128 MiB`, JVB `126 MiB / 192 MiB`, edge `14 MiB / 64 MiB`.
- 2026-05-08: immediately after the `648f561` rollout, backend websocket `EOFException` / `1006` entries were visible only at the rollout reconnect timestamp (`2026-05-08T07:24:44Z`); no later `EOFException`, `1001/1006`, or slow-send warnings appeared in the next sampled log window.
- 2026-05-08: the repo previously had no OpenAPI/Swagger layer; adding tester-facing API docs required all three pieces together: `springdoc-openapi-starter-webmvc-ui`, `SecurityConfig` allow-list entries for docs paths, and `deploy/Caddyfile` proxy routes for `/v3/api-docs*` and `/swagger-ui*`, otherwise docs would work only on direct backend port and 404 behind the production edge.
- 2026-05-08: verification after the OpenAPI addition succeeded with `backend: mvn -q test` and `web: npm run typecheck`, `npm run test:run`, `npm run build`.
- 2026-05-08: chat-bound conference support exposed a subtle Java trap: `Map.of(...).getOrDefault(null, ...)` throws `NullPointerException`, so non-chat conferences need explicit `chatId == null` branches when resolving chat memberships.
- 2026-05-08: verification after the group-call integration succeeded with `backend: mvn -q test`, `web: npm run typecheck`, `npm run test:run`, and `npm run build`.
- 2026-05-08: the most visible remaining mojibake was in `ChatListPanel.tsx` and `useMessageActions.ts`; after normalizing those strings and deleting dead code in the message delete wrappers, `web: npm run typecheck`, `npm run test:run -- useMessageActions chatWorkspaceUtils ActiveChatConversation.composer`, and `npm run build` stayed green.
- 2026-05-08: Telegram-style "media and files" browsing cannot be implemented honestly from the currently loaded message slice only; it needs a server-paged attachment query per chat, otherwise older chat files disappear as soon as message history pagination/windowing changes.
- 2026-05-08: verification after adding the chat attachment browser succeeded with `backend: mvn -q -Dtest=ChatAttachmentBrowserServiceTest test`, `backend: mvn -q test`, `web: npm run typecheck`, `web: npm run test:run`, `web: npm run test:run -- api`, and `web: npm run build`.
- 2026-05-08: jumping from that browser to an old source message can be bootstrapped from the existing message page API if each attachment item also carries `messageServerOrder`; the web client can seed its first `messages/page` cursor with `serverOrder + 1` and let normal older-page pagination continue from there.
- 2026-05-08: the current source-message jump is intentionally V1: it opens the chat on a slice ending at the target message, not a richer "around message with newer context" view. Verification after the jump work succeeded with `backend: mvn -q -Dtest=ChatAttachmentBrowserServiceTest test`, `backend: mvn -q test`, `web: npm run typecheck`, `web: npm run test:run -- ChatAttachmentBrowserSheet useMessageStreamNavigation api`, and `web: npm run build`.
- 2026-05-08: the attachment-browser photo viewer could safely stay frontend-only; the only runtime bug found during implementation was that slow preview blobs could time out into a permanent error state unless the shared preview hook explicitly reset `previewError` on eventual success. Verification after the viewer overlay work succeeded with `web: npm run typecheck`, `web: npm run test:run -- ChatAttachmentBrowserSheet`, and `web: npm run build`.
- 2026-05-08: because message bodies are encrypted at rest, a Telegram-style shared-links browser cannot be implemented honestly with SQL over `chat_messages`; the cheap production-safe path is to extract HTTP(S) URLs during message send/edit and persist them separately. Verification after the link-browser work succeeded with `backend: mvn -q "-Dtest=MessageLinkExtractorTest,ChatLinkBrowserServiceTest" test`, `backend: mvn -q test`, `web: npm run typecheck`, `web: npm run test:run -- api ChatAttachmentBrowserSheet`, and `web: npm run build`.
- 2026-05-08: production logs over a 24h window stayed restart-free (`RestartCount=0`, `OOMKilled=false` for backend/edge/redis/jitsi), but they did show three real anomaly classes: repeated websocket peer disconnects (`EOFException`, `1006`), a backend `ConstraintViolationException` on `uk_user_pending_outgoing_messages_user_client`, and a Jitsi bridge allocation failure (`ColibriAllocationFailedException: Timeout`, followed by `There are no operational bridges`).
- 2026-05-08: the backend duplicate-key failure happened while persisting `user_pending_outgoing_messages`; the failing unique pair was `(user_id, client_message_id)`, which means failed-send persistence is still racy/idempotency-unsafe under retries for the same client message id.
- 2026-05-08: edge logs also showed common internet scanners probing `/.env*` and `phpinfo.php`; they currently get `200` with the SPA fallback HTML rather than a hard `404`, so this is not a secret leak but it does weaken observability and should be tightened.
- 2026-05-08: the pending-outgoing race was fixed without a native SQL upsert: `PendingOutgoingMessageService.upsertOwnPendingOutgoingMessage(...)` now locks the authenticated `UserAccount` row (`findByIdForUpdate`) before lookup/save, and targeted verification passed with `backend: mvn -q "-Dtest=PendingOutgoingMessageServiceTest,MessageServiceTest" test` plus full `backend: mvn -q test`.
- 2026-05-08: the Jitsi bridge timeout correlated with JVB-side IQ processing delays (`Took 12011 ms to process an IQ`, `ProbeController ... timeout`) rather than with container restarts or OOM; the first safe mitigation in repo config is more CPU headroom for `jitsi-jvb` plus explicit websocket domain/server-id advertisement.
- 2026-05-08: `deploy/Caddyfile` had no deny rule for junk probe paths, so scanners hitting `/.env*` and `phpinfo.php` were falling through to the SPA and getting `200`. The proxy layer now owns those responses with explicit `404`s.
- 2026-05-08: production preflight still passed after the edge/Jitsi config changes (`bash deploy/preflight-prod.sh .env.prod.example`), but local Docker Compose rendering could not be re-verified in this workstation session because `docker` is not installed in the shell environment.
- 2026-05-08: production rollout to revision `eecb6d8` succeeded cleanly: `backend`, `edge`, and `jitsi-jvb` restarted onto the new revision with `RestartCount=0`, public `build-meta.json` updated, and direct checks confirmed `/.env` and `/phpinfo.php` now return `404`.
- 2026-05-08: in the first post-deploy log window there were no fresh backend duplicate-key errors and no new Jitsi `ColibriAllocationFailedException` events; only startup-time bridge churn (`Lost a bridge`) and normal client websocket peer disconnects (`EOFException`, `1006`) appeared.
- 2026-05-08: repeated production `DELETE /api/chats/{chatId}/draft -> 404` noise was avoidable client-state cleanup, not a real error; `ChatDraftService.deleteOwnDraft(...)` now deletes by `(userId, chatId)` without a chat-visibility check, and verification passed with `backend: mvn -q "-Dtest=ChatDraftServiceTest" test` plus full `backend: mvn -q test`.
- 2026-05-08: a later log check showed production was still on revision `eecb6d8`, not the newer `84829b0` draft-delete fix, so `DELETE /api/chats/{chatId}/draft -> 404` remains expected noise until the next deploy.
- 2026-05-08: the first `edge` probe hardening only covered root-style paths; nested probe variants like `/portal/phpinfo.php` and `/phpinfo/info.php` still fell through to the SPA and returned `200`.
- 2026-05-08: `deploy/Caddyfile` deny matching was then broadened to nested `/.env*`, `/.git*`, and `phpinfo*` path segments; local regex checks matched probe samples such as `/portal/phpinfo.php` and `/foo/.env` while leaving `/meet/config.js` untouched, and `bash deploy/preflight-prod.sh .env.prod.example` still passed.
- 2026-05-09: adding mailbox endpoints to `AuthController` also changed the constructor contract for `SecurityConfigTest`; the web-slice test now needs a mocked `UserMailboxService` or Spring fails the controller context before any security assertions run.
- 2026-05-09: the new mail-toggle block in `ProfileSettingsCard.tsx` initially landed with mojibake Cyrillic while adjacent legacy strings were valid UTF-8; when patching Russian UI copy from this shell, verify the saved file content itself, not only terminal rendering.
- 2026-05-09: `UpdateProfileRequest.mailEnabled` cannot stay a primitive `boolean` without silently disabling the Mail tab for older clients that omit the new field; the request/service contract now preserves the stored flag when `mailEnabled` is absent.
- 2026-05-09: verification after the optional mailbox/Mail-tab work succeeded with `backend: mvn -q "-Dtest=SecurityConfigTest,UserMailboxServiceTest" test`, `backend: mvn -q test`, `web: npm run test:run -- useWorkspaceStatus ProfileSettingsCard SidebarManagementSheets`, `web: npm run test:run`, `web: npm run typecheck`, and `web: npm run build`.
- 2026-05-09: the remaining visible workspace mojibake was in `web/src/features/chat/components/WorkspaceSidebar.tsx` (menu/search/tab copy) and `WorkspaceConversation.tsx` (loading/empty-state copy); replacing those literals with ASCII-safe `\u` escapes fixed the broken UI text without changing workspace behavior.
- 2026-05-09: on this Windows shell, direct `npm ...` calls from PowerShell fail on the disabled `npm.ps1` execution policy; `cmd /c npm run typecheck` and `cmd /c npm run build` worked for frontend verification.
- 2026-05-09: new-group auto-open behavior came from a dedicated follow-up path in `web/src/features/chat/hooks/useWorkspaceMutations.ts` (`onGroupCreated(chat, { openMenu })`) plus `NorthMessengerWorkspace.tsx` state/effect (`pendingGroupMenuOpenChatId -> setIsChatMenuOpen(true)`); removing that contract keeps post-create navigation into the new group but no longer forces the group menu open.
- 2026-05-09: verification for the group-create follow-up fix succeeded with `web: cmd /c npm run test:run -- useWorkspaceMutations` and `web: cmd /c npm run typecheck`.
- 2026-05-09: production logs on server revision `b0ee13c` showed the stack healthy (`backend/web/postgres/redis` healthy; `edge`/Jitsi/MinIO up) but repeated backend `42P18` failures in the chat media browsers: 31 hits on `GET /api/chats/{id}/attachments/browser` and 16 hits on `GET /api/chats/{id}/links/browser` over the last 24h, all failing with `ERROR: could not determine data type of parameter $2`.
- 2026-05-09: those prod browser failures line up with JPQL in `backend/src/main/java/com/north/messenger/domain/repository/ChatAttachmentRepository.java` and `ChatMessageLinkRepository.java`; both queries use nullable predicates like `(:visibleFrom is null or message.createdAt >= :visibleFrom)`, which PostgreSQL/Hibernate is currently failing to type in production.
- 2026-05-09: from this Windows workstation, SSH works via the normal `%USERPROFILE%\\.ssh` alias `pishi.ktsf.ru`; using the mirrored OneDrive desktop key path directly hit OpenSSH hostfile parsing and `UNPROTECTED PRIVATE KEY FILE` issues.
- 2026-05-09: the production media-browser `42P18` fix was to remove JPQL `:param is null` checks from `ChatAttachmentRepository.findBrowserItems(...)` and `ChatMessageLinkRepository.findBrowserItems(...)`; the services now pass explicit `applyVisibleFrom` / `applyCursor` booleans plus typed fallback values so PostgreSQL never sees an untyped null placeholder in those queries.
- 2026-05-09: verification after the media-browser query fix succeeded with `backend: mvn -q "-Dtest=ChatAttachmentBrowserServiceTest,ChatLinkBrowserServiceTest" test` and full `backend: mvn -q test`.
- 2026-05-09: auth form validation is now centralized in `web/src/features/auth/authValidation.ts`; registration blur/submit errors, disabled submit states, and the profile password-change form all share the same frontend checks that mirror backend `PasswordPolicyService`.
- 2026-05-09: concurrent typing could wipe the focused composer because `ActiveChatConversation.next.tsx` replayed stale `activeDraft` values back into the textarea during sync; skipping that write while the textarea is focused preserves the local draft.
- 2026-05-09: account deletion was erasing some direct-chat history indirectly (`deleteDirectRoomsWithFewerThanTwoParticipants`) and several renderers skipped unknown senders; preserving orphaned direct rooms and substituting `Deleted user` placeholders in chat/message/link/attachment paths keeps history visible after peer deletion.
- 2026-05-09: conference invite-link refresh from the UI was reusing the existing code because `NorthMessengerWorkspace.tsx` called `createConferenceInviteLink` without `refresh=1`; once a code exists, the client now sends the refresh flag explicitly.
- 2026-05-09: editing a message that still contains links could hit the unique key `uk_chat_message_links_message_position` because `ChatMessageLinkService.syncLinks(...)` deleted and reinserted rows in one transaction without flushing the delete first.
- 2026-05-09: `EmailVerificationEmailNotifier` was swallowing mail-sender failures in its `AFTER_COMMIT` listener, so `/api/auth/*email-verification*` could still return `202` while delivery only failed in logs; the listener now lets delivery exceptions propagate.
- 2026-05-09: chat-list failed-send state is now surfaced from `pendingOutgoingMessages` with a dedicated list indicator, profile name/about fields show backend-aligned `40/160` character counters with long-name wrapping, user search ordering is exact > prefix > partial in `UserAccountRepository.searchByUsernameOrDisplayName(...)`, and search dropdowns close on blur unless focus moves onto a result action.
- 2026-05-09: verification after the QA bugfix batch succeeded with `web: cmd /c npm run test:run`, `web: cmd /c npm run typecheck`, `web: cmd /c npm run build`, `backend: mvn -q "-Dtest=InviteLinkServiceTest,EmailVerificationEmailNotifierTest,ChatMessageLinkServiceTest,UserAccountRepositoryTest" test`, and full `backend: mvn -q test`.
- 2026-05-09: group-ban management previously had only `POST /api/chats/{chatId}/bans`; fixing the re-add-after-unban QA bug required a real unban/read path (`GET` + `DELETE /api/chats/{chatId}/bans...`) plus a frontend banned-users section in the group members UI so removing the `chat_room_bans` row is actually reachable from the product.
- 2026-05-09: failed direct messages could visually collide with the bottom-right timestamp/status block because direct-chat meta is absolutely positioned; when `message.status.state === FAILED`, the bottom meta now switches to normal flow (`.is-inline`) and the failure row can wrap, preventing retry/timestamp overlap.
- 2026-05-09: verification after the unban/retry-layout follow-up succeeded with `backend: mvn -q "-Dtest=ChatServiceTest" test`, `web: cmd /c npm run test:run -- ActiveChatConversation.composer useWorkspaceMutations api SidebarManagementSheets`, `web: cmd /c npm run typecheck`, then full `web: cmd /c npm run test:run`, `web: cmd /c npm run build`, and `backend: mvn -q test`.
- 2026-05-09: local attachment upload/download paths depend on `APP_MEDIA_MESSAGE_ATTACHMENTS_MINIO_PUBLIC_ENDPOINT=http://localhost:5173/storage`; Vite must strip the `/storage` prefix before proxying to MinIO or presigned message-file URLs fail with MinIO `403 AccessDenied`, which breaks both direct-chat and group-chat file attachments on localhost.
- 2026-05-09: `web/public/push-sw.js` was suppressing all push notifications whenever any North Messenger tab existed, even a hidden background tab; only visible/focused same-origin clients should suppress the generic service-worker notification.
- 2026-05-09: backend password change logic already revokes sessions and updates the hash correctly; the QA regression was covered by adding `AuthServiceTest.changePasswordShouldRequireNewPasswordForNextLogin`, which proves old credentials fail and the new password can log in immediately after the change.
- 2026-05-09: verification after the attachment/push/password follow-up succeeded with `web: cmd /c npm run test:run -- pushServiceWorker viteConfig pushNotifications ProfileSettingsCard`, `backend: mvn -q "-Dtest=AuthServiceTest" test`, `web: cmd /c npm run typecheck`, `web: cmd /c npm run test:run`, `web: cmd /c npm run build`, and `backend: mvn -q test`.
- 2026-05-09: draft-only direct chats were still filtered out in `NorthMessengerWorkspace.tsx` by `lastMessageAt !== null`; the safe fix was to track per-chat draft activity in `useChatDrafts.ts`, keep those chats listable in `useWorkspaceStatus.ts`, and sort idle chat order by `max(chat.updatedAt, draft.updatedAt)` when no search is active.
- 2026-05-09: the visible edited-time bug came from `ActiveChatConversation.next.tsx` labeling a message as edited but still formatting `createdAt`; the timeline meta now uses `editedAt ?? createdAt`, and the desktop/mobile workspace breakpoint was lowered from `960px` to `820px` so the conversation pane no longer disappears around half-width desktop windows.
- 2026-05-09: chat-list reaction indication now rides on the existing chat-summary websocket path instead of a new client-only side channel: backend `ChatSummaryResponse` gained `lastMessageHasReactions`, `MessageRealtimeEventListener` now also calls `chatService.notifyChatUpdated(...)` after reaction broadcasts, and `ChatListPanel.tsx` shows a small reaction pill when the latest visible message has reactions.
- 2026-05-09: `ChatListPanel.tsx` had to be fully rewritten in ASCII-safe form because JSX attributes like `title="\u0415..."` keep the backslash escapes literally; to emit real Cyrillic in DOM attributes, use expression form `title={"\u0415..."}`.
- 2026-05-09: while adding draft-activity tracking, `useChatDrafts.ts` regressed because the unload effect depended on an Effect Event and re-flushed drafts on ordinary rerenders; the fix was to keep the `pagehide/beforeunload` effect non-reactive (`[]`) and to route empty composer changes through `syncDraftLocally(chatId, "")` so cleared drafts disappear immediately from local/query state.
- 2026-05-09: verification after the chat-list/chat-layout follow-up succeeded with `web: cmd /c npm run typecheck`, `web: cmd /c npm run test:run -- useChatDrafts ChatListPanel chatState useWorkspaceStatus ActiveChatConversation.next`, full `web: cmd /c npm run test:run`, `web: cmd /c npm run build`, `backend: mvn -q "-Dtest=ChatServiceTest,ChatOpenServiceTest,WorkspaceSearchServiceTest,MessageRealtimeEventListenerTest" test`, and full `backend: mvn -q test`.
- 2026-05-09: direct-chat `delete for self` was still effectively an unhide because `createDirectChat(...)` and `MessageCommandService.sendMessage(...)` cleared `user_deleted_chats` without changing direct-history visibility; the safe fix was to reopen only for the acting user, move that direct membership's `joinedAt` forward to the user's `deletedAt`, and then reuse `joinedAt` as the direct-chat visible-history cutoff in summary/message/link/attachment/reply paths.
- 2026-05-09: verification after the direct-chat reopen/history-cutoff fix succeeded with `backend: mvn -q "-Dtest=ChatServiceTest,MessageServiceTest,ChatAttachmentBrowserServiceTest,ChatLinkBrowserServiceTest" test` and full `backend: mvn -q test`.
- 2026-05-09: forward-message UX already had working mutation/error plumbing, but the dedicated forward sheet showed no local state; `SidebarUtilitySheets.tsx` now renders an inline pending status and inline forwarding error so the user sees progress/failure where the action started, while successful forward still resolves by opening the target chat.
- 2026-05-09: verification after the forward-sheet status fix succeeded with `web: cmd /c npm run test:run -- SidebarUtilitySheets`, `web: cmd /c npm run typecheck`, and `web: cmd /c npm run build`.
- 2026-05-09: pinned messages are no longer modeled as a single `chat_rooms.pinned_message_id`; `V5__add_chat_pinned_messages.sql` introduces `chat_pinned_messages`, `ChatService.updatePinnedMessage(...)` now toggles one message within that collection, and the legacy room-level pinned fields are kept only as a compatibility alias pointing at the newest remaining pin.
- 2026-05-09: the pinned banner now supports multiple messages end-to-end: backend `ChatSummaryResponse` exposes `pinnedMessages`, snippets carry `serverOrder`, `NorthMessengerWorkspace.tsx` tracks the active pinned index/id, and mobile `Перейти` reuses the seeded message-jump path so older pinned messages can be fetched and scrolled into view instead of only working for already loaded history.
- 2026-05-09: verification after the multi-pin/mobile pinned follow-up succeeded with `web: cmd /c npm run typecheck`, full `web: cmd /c npm run test:run`, `web: cmd /c npm run build`, `backend: mvn -q "-Dtest=ChatServiceTest,MessageServiceTest,ChatOpenServiceTest,WorkspaceSearchServiceTest" test`, and full `backend: mvn -q test`.
- 2026-05-09: production `PROF-006` verification is no longer only inferred from code: `/opt/messenger-app/.env.prod` has email verification enabled against `smtp.timeweb.ru:465`, SMTP login and `RCPT TO` both succeeded for `noreply@ktsf.ru` and a plus-address alias, and a live temp-account flow on `https://pishi.ktsf.ru` (`register -> POST /api/auth/me/email-verification -> delete account`) returned `200/202/204`, rotated verification tokens from `1|1` to `2|1`, and produced no mail-related backend exceptions since the test start.
- 2026-05-10: production is already on revision `3880966` (`Fix QA issues across auth chat groups and profile`, built at `2026-05-09T20:16:22.550Z`), so fresh log anomalies after that time are post-deploy, not stale pre-fix noise.
- 2026-05-10: a 24h prod log sweep after deploy showed no backend `ERROR` lines, but there is still a live attachment-upload failure outside backend: Caddy/MinIO logged `12` repeated `403 PUT /storage/message-attachments/...` requests between `2026-05-10T05:12:30Z` and `2026-05-10T15:42:15Z`; the requests reached MinIO through presigned URLs with `X-Amz-SignedHeaders=content-type;host` and browser `Content-Type: image/jpeg`, so the remaining bug is on the presigned upload path, not the chat-message API.
- 2026-05-10: other prod anomalies in the same 24h window were lower-severity: `16` edge `status=0` client-aborted requests (mostly `GET /api/chats` plus a couple `/api/auth/refresh` and typing polls), `10` ordinary `401 POST /api/auth/refresh` responses, a single probe `404 GET /.git/config`, backend startup warning about ephemeral VAPID keys (`APP_PUSH_VAPID_*` unset, existing subscriptions will need refresh after restart), and a handful of Jitsi disconnect warnings (`recv() failed` / `Participant no longer active`) that look like short call/session churn rather than a persistent outage.
- 2026-05-10: the live `403 PUT /storage/message-attachments/...` prod failure came from signing MinIO presigned URLs against the public endpoint path prefix (`/storage`) while both Caddy `handle_path /storage/*` and the Vite dev proxy strip that prefix before proxying upstream; `MinioChatAttachmentStorage` now signs against the public host root and only rewrites the returned URL back under `/storage/...`, while the Vite `/storage` proxy keeps the original `Host` header (`changeOrigin: false`) so the signed `host` header still matches.
- 2026-05-10: deployment `7f15f0056c60a84f7384e2ab97aa1654e2d52e05` (`Fix MinIO presigned URL conversion for deploy build`) completed successfully after a follow-up compile-only fix for `Presigned*ObjectRequest.url()` being `URL`, not `URI`; local verification that worked before rollout was `backend: mvn -q "-Dtest=MinioChatAttachmentStorageTest,ChatAttachmentServiceTest" test`, `backend: mvn -q test`, `web: cmd /c npm run test:run -- viteConfig`, `web: cmd /c npm run typecheck`, and `web: cmd /c npm run build`.
- 2026-05-10: post-deploy prod smoke for attachment uploads succeeded end-to-end on `https://pishi.ktsf.ru`: a temp account registered, created an empty group chat, called `POST /api/chats/{chatId}/attachments/initiate`, then `PUT` a 4-byte `image/jpeg` to the returned `https://pishi.ktsf.ru/storage/message-attachments/...` presigned URL and received `200`; fresh edge logs now show that request as `status=200`, backend logs stayed free of new attachment exceptions, and the temp user `qa10203113` was confirmed deleted afterward (`select username from app_users where username = 'qa10203113';` returned no rows).
- 2026-05-11: prod log sweep on revision `7f15f0056c60a84f7384e2ab97aa1654e2d52e05` found the stack restart-free (`restart=0`, `oom=false` across backend/edge/web/postgres/redis/minio/jitsi), with no backend `ERROR` lines in the last 24h; the only backend warnings were startup-time ephemeral VAPID keys and one `Slow chat summary broadcast ... durationMs=325`.
- 2026-05-11: `edge` hardening still misses some `phpinfo` probe shapes. In the last 24h, `/?phpinfo=1`, `/_profiler/phpinfo`, `/phpinfo`, `/symfony/_profiler/phpinfo`, and `/admin_phpinfo.php` all reached the SPA and returned `200`, while `.env`/`.git` probes and direct `phpinfo.php` paths still returned `404`. The current `deploy/Caddyfile` `@blocked_phpinfo` regex only catches `.php` or slash-suffixed variants after a `phpinfo` path segment, not plain `/phpinfo`, query-only probes, or embedded names like `admin_phpinfo.php`.
- 2026-05-11: the `phpinfo` edge hardening fix is now split into two matchers in `deploy/Caddyfile`: a broader case-insensitive path regex for `/phpinfo`, nested `/.../phpinfo`, and embedded `*phpinfo*.php` probe names, plus a dedicated `query phpinfo=*` block for `/?phpinfo=...`. Local verification passed with targeted URI match checks, remote `docker exec messenger-edge ... caddy adapt --config /tmp/Caddyfile.test --adapter caddyfile`, and `bash deploy/preflight-prod.sh .env.prod.example`.
- 2026-05-11: production push notifications were disabled by config, not by the client: `/opt/messenger-app/.env.prod` had `APP_PUSH_ENABLED=false`, the running backend had `APP_PUSH_ENABLED=false` and no configured `APP_PUSH_VAPID_*`, so `PushVapidKeyService` would only fall back to ephemeral keys. The prod fix was operational: generate a stable P-256 VAPID pair, store `APP_PUSH_ENABLED=true`, `APP_PUSH_SUBJECT=mailto:noreply@ktsf.ru`, `APP_PUSH_VAPID_PUBLIC_KEY`, and `APP_PUSH_VAPID_PRIVATE_KEY` in `.env.prod`, then recreate only `backend`. Verification: `bash deploy/preflight-prod.sh .env.prod`, `docker exec messenger-app-backend-1 printenv APP_PUSH_ENABLED APP_PUSH_SUBJECT`, and fresh backend startup logs with no ephemeral-VAPID warning.
- 2026-05-11: the active-devices-multiplying report for user `north` was not caused by `/api/auth/refresh` cloning sessions. In code, only `login/register` call `createSessionResponse(...)`, while `refresh(...)` keeps the same `user_sessions.id` and only rotates the token/expiry. Prod DB confirmed three active `north` sessions with the same coarse label `Chrome on Windows`: one long-lived session from `2026-05-07T19:16:56Z` still active on `2026-05-11T11:29:46Z`, plus extra sessions created on `2026-05-07T19:15:13Z` and `2026-05-09T07:08:26Z` that were never reused after creation. The exact product reason is that the UI lists auth sessions, not unique physical devices, and `device_name` is derived only from User-Agent, so repeated sign-ins from isolated browser contexts with the same UA collapse to duplicate-looking rows like `Chrome on Windows`.
- 2026-05-11: the sessions sheet UX now reflects that distinction explicitly: `web/src/features/chat/components/SidebarManagementSheets.tsx` renames the sheet from "active devices" to "active sessions", explains that one browser may appear multiple times after repeated sign-ins / other profiles / private windows, and adds a per-session login-created timestamp line from `createdAt`. Targeted verification passed with `web: cmd /c npm run test:run -- SidebarManagementSheets` and `web: cmd /c npm run typecheck`.
- 2026-05-13: the repo currently has no Android/mobile module. Reuse candidates are the existing React/Vite web client and the Tauri 2 shell in `web/src-tauri`, but Android work must explicitly handle platform-sensitive paths in `web/src/lib/api.ts` (cookie/session fetch + presigned MinIO transfer), `web/src/lib/realtime.ts` (STOMP websocket lifecycle), `web/src/lib/pushNotifications.ts` + `web/public/push-sw.js` (Web Push / service worker), and `ActiveConferenceConversation.tsx` (`navigator.mediaDevices`).
- 2026-05-13: current conference UX is web-embedded Jitsi, not a mobile-native call surface: `ActiveConferenceConversation.tsx` does prejoin/media-device work with `navigator.mediaDevices`, and `ManagedConferenceStage.tsx` mounts a Jitsi iframe plus `postMessage` bridge (`config.disableDeepLinking=true`, custom toolbar/event wiring). This is acceptable for web, but it is a poor fit for a high-quality Android in-app calling product without a mobile SDK/native rewrite of the call surface.
- 2026-05-13: Android planning is now captured in `docs/android.md`. The first hard architectural blocker identified during planning is auth refresh: backend `AuthController` currently writes and reads refresh tokens only through `RefreshTokenCookieService`, so Android needs an explicit mobile-safe refresh/session contract before client implementation proceeds far.
- 2026-05-13: Sprint 0 implementation created a real `android-app` React Native CLI scaffold, extracted shared TypeScript contracts into `packages/shared`, and replaced `web/src/lib/types.ts` with a thin re-export layer over that package.
- 2026-05-13: the mobile auth blocker is now implemented with `POST /api/mobile/auth/{register,login,refresh,logout}`; the contract returns refresh tokens in JSON, keeps the web cookie flow unchanged, and remains covered by auth endpoint rate limiting without requiring browser fetch-metadata headers.
- 2026-05-13: verification after Android bootstrap/mobile-auth work succeeded with `backend: mvn -q test`, `web: cmd /c npm install`, `cmd /c npm run typecheck`, `cmd /c npm run build`, and `android-app: cmd /c npm install`, `cmd /c npm run typecheck`, `cmd /c npm test -- --runInBand`.
- 2026-05-13: Sprint 1 auth/bootstrap implementation stores only the Android `refreshToken` in secure storage (`react-native-keychain`) and restores the access session on app start via `/api/mobile/auth/refresh`; the current UI already supports login/register/logout plus a read-only workspace bootstrap snapshot.
- 2026-05-13: verification after Sprint 1 auth/bootstrap work succeeded with `android-app: cmd /c npm run typecheck`, `cmd /c npm run lint`, `cmd /c npm test -- --runInBand`, plus `web: cmd /c npm run typecheck`, `cmd /c npm run build`. Native `android-app/android: cmd /c gradlew.bat assembleDebug` could not be completed in this workstation session because Android SDK is not configured (`ANDROID_HOME`/`local.properties` missing).
- 2026-05-13: Android Sprint 1 now has a real workspace shell (`Chats / Contacts / Conferences / Profile`) plus a chat thread read path that calls `GET /api/chats/{chatId}/open`, paginates older history via `/messages/page`, and acknowledges read receipts from the mobile client.
- 2026-05-13: `android-app/App.tsx` now also handles the intermediate "session restored but workspace bootstrap failed" state explicitly, so Android does not silently fall back to the auth screen when mobile auth succeeded but workspace sync needs retry.
- 2026-05-13: verification after the Android workspace/chat-read follow-up succeeded with `android-app: cmd /c npm run typecheck`, `cmd /c npm run lint`, and `cmd /c npm test -- --runInBand`.
- 2026-05-13: Android Sprint 2 now has a first live messaging loop: `android-app` subscribes to STOMP `/ws`, listens for chat/message/session queues, sends plain messages over `/app/chats/{chatId}/messages`, and merges websocket acks plus incoming messages into the thread screen.
- 2026-05-13: because RN/Jest did not resolve `@stomp/stompjs` cleanly through its export map in this workspace, Android currently carries a small local shim at `android-app/src/types/stompjs.d.ts` plus a Jest module mapper to the package UMD bundle; this keeps the mobile workspace typecheck/test green without changing the backend/web realtime contract.
- 2026-05-13: the current Android send/retry implementation is intentionally in-memory only. It already shows optimistic `SENDING/FAILED` messages and explicit retry, but it does not yet persist failed sends through `user_pending_outgoing_messages` across app restarts.
- 2026-05-13: verification after the Android realtime/composer follow-up succeeded with `android-app: cmd /c npm install`, `cmd /c npm run typecheck`, `cmd /c npm run lint`, and `cmd /c npm test -- --runInBand`.
- 2026-05-13: Android pending sends now persist through backend `user_pending_outgoing_messages`: `App.tsx` upserts/deletes pending rows through mobile-authenticated REST calls, normalizes bootstrapped stale `SENDING` rows to `FAILED` after restart, and `ChatThreadScreen.tsx` merges those recovered pending messages back into the visible thread.
- 2026-05-13: chat open/page cleanup matters on Android too, not only on web. `confirmedPendingOutgoingClientMessageIds` from `/open` and `/messages/page` are now used to delete already-confirmed pending rows so recovered local placeholders do not survive after the server has accepted the message.
- 2026-05-13: verification after the Android pending-outgoing recovery follow-up succeeded with `android-app: cmd /c npm run typecheck`, `cmd /c npm run lint`, and `cmd /c npm test -- --runInBand`.
- 2026-05-13: Android reply/edit did not need new backend surface. Reply now reuses the existing websocket send contract with `replyToMessageId` and pending-outgoing reply snippets, while edit uses the existing `PUT /api/chats/{chatId}/messages/{messageId}` contract and merges the returned message back into the thread locally.
- 2026-05-13: focused mobile coverage now includes `ChatThreadScreen.test.tsx`, which asserts that reply sends carry `replyToMessageId` through `sendPlainMessage(...)` and own-message edits go through `updatePlainMessage(...)` instead of the websocket send path.
- 2026-05-13: verification after the Android reply/edit follow-up succeeded with `android-app: cmd /c npm run typecheck`, `cmd /c npm run lint`, and `cmd /c npm test -- --runInBand`.
- 2026-05-13: Android reactions reused existing server contracts cleanly: mobile now calls `PUT /api/chats/{chatId}/messages/{messageId}/reactions`, listens to `/user/queue/message-reactions`, and updates the visible thread without new backend endpoints.
- 2026-05-13: while wiring Android realtime reactions, `ChatThreadScreen` exposed a real mobile race: the initial `/open` load effect was indirectly rerunning on parent rerenders because `App.tsx` passed a freshly filtered `pendingOutgoingMessages` array each render. The screen now keeps pending-message sync separate from chat-open loading, so reaction/session rerenders do not wipe the thread.
- 2026-05-13: Android reaction-state hardening now keeps latest reaction events in a small in-memory override map so a reaction that arrives before `/open` or older-page hydration finishes is still applied when that message becomes visible.
- 2026-05-13: verification after the Android reactions follow-up succeeded with `android-app: cmd /c npm run typecheck`, `cmd /c npm run lint`, and `cmd /c npm test -- --runInBand`.
- 2026-05-13: Android typing now uses the existing backend contracts only: mobile publishes `/app/chats/{chatId}/typing`, subscribes to `/topic/chats.{chatId}.typing` for the active chat, and shows a local typing indicator without new backend endpoints.
- 2026-05-13: the current Android typing implementation is intentionally scoped to the open thread only; `App.tsx` keeps the websocket connection stable and switches typing subscriptions via `replaceSubscribedChatIds([activeChatId])` instead of reconnecting on every chat open/close.
- 2026-05-13: adding Android typing introduced real timer cleanup risk in tests and screen lifecycle. The safe pattern here is explicit timeout maps/idle-timeout refs in `ChatThreadScreen.tsx` plus explicit renderer unmount in `ChatThreadScreen.test.tsx`, otherwise Jest leaves open handles after the new typing timers are added.
- 2026-05-13: verification after the Android typing follow-up succeeded with `android-app: cmd /c npm run typecheck`, `cmd /c npm run lint`, and `cmd /c npm test -- --runInBand`.
- 2026-05-17: `AuthService.deleteAccount(...)` previously hard-deleted `app_users`, which cascaded into disappearing direct-chat history after peer deletion; the safe fix was to anonymize the row in place, revoke sessions, and map deleted accounts back through placeholder participant/profile DTOs.
- 2026-05-17: group history loss after leave/ban/remove was caused by physically deleting `chat_participants` rows and then deriving visibility from row existence. `chat_participants.left_at` now keeps a history-only state, while message/link/attachment/reply queries also honor an upper visibility bound from the leave/ban timestamp.
- 2026-05-17: `MessageReactionChangedEvent` now carries the actor user id and `MessageReactionService.broadcastReactionChanged(...)` skips that actor, so reaction authors do not receive their own websocket reaction event back.
- 2026-05-17: the web reaction/CPU regression came from double preview writes for the same latest message in `useWorkspaceEffects.ts`; removing the extra `applyChatPreviewMessage(...)` call stopped the last-message reaction feedback loop without changing the rest of the realtime flow.
- 2026-05-17: truthful email-verification resend UX required splitting delivery modes in `EmailVerificationService`: registration can still queue an `AFTER_COMMIT` event, but resend paths now call `EmailVerificationEmailSender` synchronously so SMTP failures surface to the user.
- 2026-05-17: mailbox/profile QA fixes in this batch were small contract mismatches, not rewrites: `CreateUserMailboxRequest` now rejects malformed addresses, `UpdateProfileRequest.profession` matches the frontend `160`-char limit, and `ProfileSettingsCard` renders password-change backend errors inline.
- 2026-05-17: `web/src/features/chat/components/ChatAttachmentBrowserSheet.tsx` now merges attachment and link queries for the `ALL` tab instead of showing attachments only, and `useMessageStreamNavigation.ts` adds a temporary CSS highlight after jump-to-message scroll completes.
- 2026-05-17: verification for this QA bugfix batch succeeded with `web: cmd /c npm run typecheck`, `web: cmd /c npm run test:run`, `web: cmd /c npm run build`, `backend: mvn clean test`, and `backend: mvn -q -DskipTests package`. There is still no dedicated web lint script in `web/package.json`, and no separate backend lint/typecheck command beyond Maven compile/test.
- 2026-05-17: the open follow-up QA cases around group calls and short viewports were still point fixes, not new subsystems: `VideoConferenceService.startGroupConferenceCall(...)` now trims generated titles to the `video_conferences.title` limit, `WorkspaceSidebar` handles `PageUp/PageDown` paging locally, the composer suppresses browser page-scroll keys, and `useWorkspaceStatus` makes floating mutation/query errors transient per active context.
- 2026-05-17: profile follow-up UX now surfaces the real `2..40` name rule inline and clarifies the optional Mail-tab copy; the push section no longer hides the enable action just because browser permission is currently denied, so the profile screen remains actionable after a manual browser-permission change.
- 2026-05-17: verification after this follow-up fix batch succeeded with `web: cmd /c npm run typecheck`, full `web: cmd /c npm run test:run`, `web: cmd /c npm run build`, targeted `backend: mvn -q "-Dtest=VideoConferenceServiceTest" test`, full `backend: mvn test`, and `backend: mvn -q -DskipTests package`. In this workstation session `backend: mvn clean test` failed because `backend/target/classes` was locked by a local Java process while the separate non-clean `mvn test` and package runs were green.
- 2026-05-17: the remaining QA tail (`@mentions`, forwarded-message indication, reaction indicator for non-last-message reactions) closed without new endpoints on the web side, but it did require persisted backend metadata/state for forwarded messages and per-user reaction attention.
- 2026-05-17: after `forwardedFromMessageId` was added to the websocket send contract, `web/src/lib/realtime.test.ts` needed its expected publish payload updated; the runtime behavior itself stayed compatible.
- 2026-05-17: `MessageServiceTest` needed a real `ChatRoomRepository` field plus a `peerUser` lookup stub once forwarded-message response hydration started resolving the original sender through `userAccountRepository.findById(...)`.
- 2026-05-17: verification after the mentions/forward/reaction-attention follow-up succeeded with `web: cmd /c npm run typecheck`, full `web: cmd /c npm run test:run`, `web: cmd /c npm run build`, `backend: mvn test`, and `backend: mvn -q -DskipTests package`.
- 2026-05-17: product-QA audit facts from code: group `Blocked participants` and unban are not owner-only; `ChatService.listGroupBans(...)` and `unbanGroupParticipant(...)` require moderator-or-owner access, and the web `ChatMembersPanel` renders that section for `canModerateMembers`.
- 2026-05-17: group moderation/owner follow-up changed several earlier product-audit facts: the owner can now transfer ownership to an active moderator, moderators can edit group title/avatar, and owner/moderators can delete any group message for everyone. `prejoinHistoryPolicy` remains owner-only.
- 2026-05-17: conference scheduling is still organizer-driven only. Participants can see scheduled conferences and join when the room opens, but there is still no RSVP/accept/decline state or participant-response API in the current model.
- 2026-05-17: web chat attachments now include inline audio playback plus `MediaRecorder`-based voice recording in the composer; both reuse the existing attachment upload/send flow rather than adding a dedicated backend voice-message surface.
- 2026-05-17: verification after the group-governance/audio/voice batch succeeded with targeted `backend: mvn -q "-Dtest=ChatServiceTest,MessageServiceTest" test`, full `backend: mvn test`, `backend: mvn -q -DskipTests package`, and `web: cmd /c npm run typecheck`, `web: cmd /c npm run test:run`, `web: cmd /c npm run build`.
- 2026-05-17: profile mail-toggle copy is now normalized around `Раздел «Почта»` with explicit `Открыт/Скрыт` states, and the adjacent inline name-validation text no longer renders as mojibake. Verification: `web: cmd /c npm run test:run -- ProfileSettingsCard`, `web: cmd /c npm run typecheck`.
- 2026-05-17: Android forward could not be implemented safely as UI-only because failed cross-chat forwards need to survive app restarts and retry in the target thread; the minimal compatible fix was to persist `forwardedFromMessageId` through `user_pending_outgoing_messages` and recover it back into `PendingOutgoingMessage` / `ChatMessage`.
- 2026-05-17: Android forward target selection can stay simple for now: the existing workspace bootstrap already contains all visible chats, so `ChatThreadScreen` can offer an inline target chooser without introducing a separate mobile search/navigation stack first.
- 2026-05-17: verification after the Android forward follow-up succeeded with `android-app: cmd /c npm run typecheck`, `cmd /c npm run lint`, `cmd /c npm test -- --runInBand`, `web: cmd /c npm run typecheck`, `backend: mvn -q -Dtest=PendingOutgoingMessageServiceTest test`, `backend: mvn test`, and `backend: mvn -q -DskipTests package`.
- 2026-05-17: the web composer layout was wasting horizontal space and could stretch awkwardly in compact viewports; `ActiveChatConversation.next.tsx` now groups voice + attachment into a vertical `composer-action-stack`, keeps a wider fixed-height textarea, and reduces the send-button footprint. Verification: `web: cmd /c npm run test:run -- ActiveChatConversation.composer`, `web: cmd /c npm run typecheck`, `web: cmd /c npm run build`.
- 2026-05-17: Android attachment send did not need backend changes: the existing upload-initiate endpoint and websocket `attachmentIds` contract were enough. The mobile-specific work was local picker integration, local attachment chip state, upload-before-send sequencing, and attachment-only composer submission.
- 2026-05-17: `react-native-document-picker@9.3.1` works for the current Android attachment pass, but npm now marks it deprecated because the package was renamed; this is acceptable for the current increment, but it should be migrated before the Android surface grows further.
- 2026-05-17: verification after the Android attachment follow-up succeeded with `android-app: cmd /c npm install react-native-document-picker@9.3.1`, `cmd /c npm run typecheck`, `cmd /c npm run lint`, and `cmd /c npm test -- --runInBand`.
- 2026-05-17: Android message history previously exposed attachment count only. `ChatThreadScreen.tsx` now renders attachment rows and opens them through `GET /api/chats/{chatId}/attachments/{attachmentId}/download-url` + `Linking.openURL(...)`, which keeps the mobile read-path aligned with the existing backend contract without new endpoints.
- 2026-05-17: verification after the Android attachment open/download follow-up succeeded with `android-app: cmd /c npm run typecheck`, `cmd /c npm run lint`, and `cmd /c npm test -- --runInBand`.
- 2026-05-17: Android attachment share can reuse the same `download-url` contract as open; `ChatThreadScreen.tsx` now resolves the presigned URL on demand and passes it into the native share sheet with `Share.share(...)`, avoiding a second attachment-transfer path.
- 2026-05-17: verification after the Android attachment share follow-up succeeded with `android-app: cmd /c npm run typecheck`, `cmd /c npm run lint`, and `cmd /c npm test -- --runInBand`.
- 2026-05-17: Android image preview also fit into the same attachment contract. `ChatThreadScreen.tsx` now treats `image/*` attachments differently from documents: it resolves the backend `download-url`, shows the image inside a local overlay, and only uses `Linking.openURL(...)` when the user explicitly asks to open it externally.
- 2026-05-17: verification after the Android image-preview follow-up succeeded with `android-app: cmd /c npm run typecheck`, `cmd /c npm run lint`, and `cmd /c npm test -- --runInBand`.
- 2026-05-17: a real native Android build is still blocked by local workstation setup, not by TypeScript/Jest code. `android-app/android: cmd /c gradlew.bat assembleDebug` currently fails with `SDK location not found`, which requires either `ANDROID_HOME` or `android/local.properties` (`sdk.dir=...`) on this machine.
- 2026-05-17: after the attachment tranche, the next safest Android increment was not FCM yet. The current backend push stack is Web Push/VAPID-specific (`user_push_subscriptions`, browser subscription endpoints, VAPID delivery) and is not a drop-in native FCM token pipeline, so Android moved next on `search/archive/block` using already-existing server contracts.
- 2026-05-17: Android workspace is no longer read-only for those basics: `WorkspaceHomeScreen.tsx` now calls backend search, archives/restores chats, and blocks/unblocks users while `App.tsx` patches `workspace` state locally so the mobile shell reflects the mutation immediately after the server call returns.
- 2026-05-17: verification after the Android workspace search/archive/block follow-up succeeded with `android-app: cmd /c npm run typecheck`, `cmd /c npm run lint`, and `cmd /c npm test -- --runInBand`.
- 2026-05-17: the next mobile-safe workspace increment was contact/direct-chat actions, not a new navigation layer. `WorkspaceHomeScreen.tsx` now exposes `Message`, `Add contact`, `Remove`, `Block`, and `Unblock` pills, while `App.tsx` uses existing backend `/api/chats/direct` and `/api/users/contacts*` endpoints and patches `workspace.contacts` / `workspace.chats` locally on success.
- 2026-05-17: verification after the Android contact/direct-chat follow-up succeeded with `android-app: cmd /c npm run typecheck`, `cmd /c npm run lint`, and `cmd /c npm test -- --runInBand`.
- 2026-05-17: Android conference detail/open fit the same incremental pattern: `WorkspaceHomeScreen` opens a dedicated `ConferenceDetailScreen`, `App.tsx` patches `workspace.conferences` / `archivedConferences` locally after start/end/cancel/presence mutations, and join currently hands off to the external Jitsi URL instead of embedding a native call surface yet.
- 2026-05-17: the first Android conference-detail test failure was environment-driven, not a product bug: Jest resolved `Platform.OS` to a non-Android host, so the correct assertion was to derive the expected join URL from `JITSI_BASE_URL` rather than hardcode `10.0.2.2`.
- 2026-05-17: verification after the Android conference detail/open follow-up succeeded with `android-app: cmd /c npm run typecheck`, `cmd /c npm run lint`, and `cmd /c npm test -- --runInBand`.
- 2026-05-17: Android conference invite links did not need new backend work. `ConferenceDetailScreen.tsx` now reuses `/api/invite-links/conferences/{id}`, keeps the generated URL local to the active detail screen, and hands it off through `Share.share(...)`; no workspace bootstrap shape change was required.
- 2026-05-17: verification after the Android conference invite-share follow-up succeeded with `android-app: cmd /c npm run typecheck`, `cmd /c npm run lint`, and `cmd /c npm test -- --runInBand`.
- 2026-05-17: after the local SDK path was supplied, the real Android build blocker moved from environment to dependencies: `react-native-document-picker@9.3.1` failed on RN 0.85 Android with missing `GuardedResultAsyncTask`, so the attachment picker had to migrate to `@react-native-documents/picker`.
- 2026-05-17: the new picker migration was small but not drop-in: default import became named imports, cancel handling moved to `isErrorWithCode(...)` + `errorCodes.OPERATION_CANCELED`, and old `copyTo` usage was replaced with explicit `keepLocalCopy(...)` before upload.
- 2026-05-17: this workstation now has a verified Android native build path via `android-app/android/local.properties -> D:/programs/coding/android-sdk`; successful checks included `android-app: cmd /c npm run typecheck`, `cmd /c npm run lint`, `cmd /c npm test -- --runInBand`, and `android-app/android: cmd /c gradlew.bat assembleDebug`.
- 2026-05-17: for the current RN 0.85 toolchain, Gradle also auto-installed additional Android components during first native build (`build-tools 35.0.0`, `cmake 3.22.1`) even though the app compiles against SDK 36; this is normal toolchain behavior, not a project bug.
- 2026-05-17: native Android verification is now beyond compile-only on this workstation: AVD `BooksyTablet` booted successfully, `adb install -r app-debug.apk` succeeded, and `adb shell am start -W -n com.north.messenger.android/.MainActivity` resumed the app with a live process (`pidof -> 1940` during the check).
- 2026-05-17: Android mobile auth never needed a backend change for email login: `AuthService.login(...)` already resolves `request.username()` through `findUserByLoginIdentifier(...)`, so the Android fix was UI-only in `AuthScreen.tsx` — explicit `Email or username` labeling plus a dark messenger-aligned palette instead of the old beige bootstrap styling.
- 2026-05-17: Android `src/config.ts` now supports both local-emulator and shared-server dev targets. The current default `devTarget` is `remote`, so Metro-built emulator sessions talk to `https://pishi.ktsf.ru` / `/meet`; live `build-meta.json` on that host responded during the switch with revision `68b114849418ebe0b17df0337a63168d0098be99`.
- 2026-05-17: production was not down during the Android sign-in incident: server containers stayed `Up`/`healthy`, and edge logs showed Android `okhttp/4.9.2` requests hitting `POST /api/mobile/auth/login` with `401` while browser `POST /api/auth/login` succeeded with `200` a few seconds later. The immediate mobile hardening was to add `Show/Hide` password visibility on `AuthScreen.tsx` so emulator input can be verified against hidden extra characters.
- 2026-05-17: two Android chat-shell issues were local client bugs, not server regressions: `@stomp/stompjs` on Hermes needed a bootstrap `TextDecoder` polyfill (`fast-text-encoding` in `android-app/index.js`), and `ChatThreadScreen.tsx` was updating parent `App` state from inside `setMessages(...)` updaters via pending-message callbacks. The fix was to move message-list mutations onto a local `messagesRef`/`applyMessagesUpdate(...)` path and trigger workspace persistence after the state update instead of inside the updater.
- 2026-05-17: Android composer on 320dp screens needed a separate responsive pass: the long offline placeholder plus wide `Send` button caused the input to collapse and overlap the system nav area. `ChatThreadScreen.tsx` now uses a short placeholder, narrower action buttons, `minWidth: 0` on the multiline input, and safe-area bottom padding from `useSafeAreaInsets()`. `ChatThreadScreen.test.tsx` now wraps screen renders in `SafeAreaProvider`.
- 2026-05-17: the Android “messages do not send / realtime reconnecting” incident was a websocket auth contract mismatch, not a dead server. Production edge returned `101` for `/ws`, but the backend saw unauthenticated STOMP sessions. The real fix was backend-side: `WebSocketAuthChannelInterceptor` now resolves `Authorization` case-insensitively from STOMP native headers, and `WebSocketConfig` also stores handshake auth as a fallback for non-browser clients. Direct production verification succeeded with a raw `wss://pishi.ktsf.ru/ws` STOMP `CONNECT` using lowercase `authorization:Bearer ...`, which returned `CONNECTED`.
- 2026-05-17: production edge did not forward websocket-handshake `Authorization` through to Spring during this incident, so handshake-header fallback alone is not sufficient on `pishi.ktsf.ru`; STOMP native header handling is still the primary auth path. Android now also creates websocket transports with an explicit handshake `Authorization` header anyway, which remains useful for local/non-proxied environments.
- 2026-05-17: verification for the Android websocket-auth fix succeeded with local `backend: mvn -q "-Dtest=WebSocketAuthChannelInterceptorTest,WebSocketConfigTest" test`, `android-app: cmd /c npm run typecheck`, `cmd /c npm run lint`, `cmd /c npm test -- --runInBand`, plus a live production backend rollout (`docker compose --env-file .env.prod -f docker-compose.prod.yml up -d --build backend`) and raw STOMP probe against `wss://pishi.ktsf.ru/ws`.
- 2026-05-18: Android message send is currently hard-coupled to a live STOMP session. `android-app/src/lib/plainMessages.ts` calls only `sendMessageRealtime(...)`, `android-app/src/lib/realtime.ts` rejects with `503 Realtime connection is unavailable` whenever the websocket is not fully ready, and the backend no longer exposes `POST /api/chats/{chatId}/messages` in `MessageController`. In practice, any mobile `Realtime reconnecting` state is also a full message-send outage, not just a degraded indicator.
- 2026-05-18: the first safe Android fix for that outage was not REST fallback but transport queueing. `android-app/src/lib/realtime.ts` now keeps pending send requests across websocket reconnects, replays unpublished/unacknowledged messages in `onConnect`, and only fails them on auth failure, connection teardown, or a longer reconnect timeout. Verification: `android-app: cmd /c npm test -- --runInBand src/lib/realtime.test.ts`, full `cmd /c npm test -- --runInBand`, `cmd /c npm run typecheck`, and `cmd /c npm run lint`.
- 2026-05-18: live BooksyTablet verification against `https://pishi.ktsf.ru` still reproduced the outage after queueing: the Android chat stayed on `Realtime reconnecting`, an optimistic direct-chat send moved into `user_pending_outgoing_messages` (`clientMessageId=android-1779056517062-zoqi2pjh`), then timed out to `FAILED`, while `GET /api/chats/aecd8e07-1cd6-4090-aadb-d9232ca781cd/messages` stayed empty. Cleanup for the check was `DELETE /api/messages/pending-outgoing/{clientMessageId}` plus emulator app stop.
- 2026-05-18: the exact Android realtime root cause was STOMP text-frame framing on React Native Android, not websocket auth or reconnect policy. `@stomp/stompjs` sent `CONNECT`, but with text websocket frames Spring never answered `CONNECTED`; the client hit its 10s connection timeout and kept cycling `Realtime reconnecting`. Switching the Android STOMP client to `forceBinaryWSFrames: true` immediately produced `CONNECTED`, `Realtime live`, and a real direct-chat send (`clientMessageId=android-1779057560216-zu42v7l0`, message `281d9e72-3773-4317-af4f-91715d006562`), which was then deleted for cleanup. Verification after the fix: `android-app: cmd /c npm test -- --runInBand src/lib/realtime.test.ts`, `cmd /c npm run typecheck`, plus live BooksyTablet send/delete against `https://pishi.ktsf.ru`.
