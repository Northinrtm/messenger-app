## Findings

- 2026-05-06: `docs/ai/*` project-memory files were missing and had to be bootstrapped.
- 2026-05-06: worktree already contains an active migration from `EncryptedMessagePayload*` to `PlainMessagePayload*`.
- 2026-05-06: a hidden backend dependency on the old message schema remained in `MessageReceiptRepository` unread queries (`message.encryptionScheme`); removing it was required for full `mvn test` coverage.
- 2026-05-06: verification succeeded with `backend: mvn -q test` and `web: npm run typecheck`, `npm run test:run`, `npm run build`.
- 2026-05-07: `deploy/bootstrap-prod-env.sh` and `deploy/preflight-prod.sh` needed explicit MinIO defaults/validation after compose switched attachment storage from local volume to MinIO.
- 2026-05-07: verification succeeded after attachment/storage refactor with `backend: mvn -q test`, `web: npm run typecheck`, `web: npm run test:run`, `web: npm run build`, and `bash deploy/preflight-prod.sh .env.prod.example`.
- 2026-05-07: direct MinIO transfer also required a public endpoint (`APP_MEDIA_MESSAGE_ATTACHMENTS_MINIO_PUBLIC_ENDPOINT`) plus edge/dev proxying on `/storage`; signing internal `http://minio:9000` URLs is not browser-usable.
- 2026-05-07: after direct MinIO transfer stabilized, the old multipart proxy endpoints (`POST /api/chats/{chatId}/attachments`, `GET /api/chats/{chatId}/attachments/{id}`) and filesystem storage backend were dead compatibility code and could be removed safely.
- 2026-05-07: message-at-rest encryption touched more than `sendMessage`; preview rendering, reply snippets, websocket dispatch, and query hydration all needed a shared decrypt path because `ChatMessage` no longer carries plaintext from JPA.
- 2026-05-07: verification succeeded after encrypted message persistence with `backend: mvn -q test`, `web: npm run typecheck`, `web: npm run test:run`, `web: npm run build`, and `bash deploy/preflight-prod.sh .env.prod.example`.
- 2026-05-07: after fresh-start-only policy was confirmed, Flyway migrations were squashed back to a single `V1__baseline.sql`; backend tests and production preflight still passed on the flattened schema.
- 2026-05-07: verification also stayed green after removing attachment proxy/filesystem compatibility paths: `backend: mvn -q test`, `web: npm run typecheck`, `web: npm run test:run`, `web: npm run build`, `bash deploy/preflight-prod.sh .env.prod.example`.
- 2026-05-07: `deploy/remote-update.sh` originally cleaned all untracked files and could delete server-owned `.env.prod`; the script now excludes `ENV_FILE` from dirtiness checks and `git clean` so bootstrap/preflight do not silently rotate or lose production secrets.
- 2026-05-07: GitHub deploy workflow logic was ported into a local Windows operator trigger (`deploy-prod-button.ps1`), which uploads `remote-update.sh`, tails remote logs, and verifies public `build-meta.json` without GitHub Actions.
- 2026-05-07: manual deploy trigger verification covered `powershell [scriptblock]::Create(...)` parsing for `deploy/deploy-prod-button.ps1`, `bash -n deploy/remote-update.sh`, `bash deploy/preflight-prod.sh .env.prod.example`, and `backend: mvn -q -Dtest=ProductionDeploymentConfigTest test`.
- 2026-05-07: local Docker/WSL startup exposed a real Postgres mapping bug: `chat_message_keys.encrypted_dek` was created as `bytea`, but JPA marked the field as `@Lob` and Hibernate expected `oid`. Fix was to remove `@Lob` and map encrypted byte arrays explicitly as `VARBINARY` in `ChatMessage` and `ChatMessageKey`.
- 2026-05-07: active-chat history hydration was clearing unread counts too early in the client because `useWorkspaceEffects` always pushed the last cached message through preview sync with unread mode `clear`; unread must stay server-driven until `isActiveChatOpen` becomes true.
- 2026-05-07: workspace navigation is expected to restore the current chat after refresh; premature read semantics must be solved in receipt/visibility logic, not by suppressing restored `activeChatId`.
- 2026-05-07: sender-side delivery UI was ambiguous because `DELIVERED` and `READ` were too close visually; current contract is `DELIVERED = one check` and `READ = two checks`, with labels still carrying receipt counts.
- 2026-05-07: fresh VPS deployment needed official Docker CE + Compose plugin/buildx on Ubuntu; older distro `docker.io/docker-compose` was not enough for the current image build path.
- 2026-05-07: the server IP hit Docker Hub anonymous rate limits for `jitsi/*` and `minio/minio`; preloading those images from the operator workstation and then running `deploy/remote-update.sh` kept the stack fully self-hosted on the VPS.
- 2026-05-07: production verification succeeded on `https://pishi.ktsf.ru/`, `https://pishi.ktsf.ru/meet/`, and `https://pishi.ktsf.ru/build-meta.json` after a clean `docker compose down --remove-orphans` and redeploy to revision `df8cff9`.
- 2026-05-07: on the 2 GB RAM + 2 GB swap VPS, the hardened prod stack stayed within limits after startup; sampled runtime usage showed backend about 427 MiB / 640 MiB and Jitsi JVB about 156 MiB / 192 MiB.
- 2026-05-07: a real production backend restart happened under normal auth flow with `java.lang.OutOfMemoryError: Metaspace`; the old `-XX:MaxMetaspaceSize=128m` was too low for the Spring app under live load.
- 2026-05-07: the production JVM hotfix was to raise backend metaspace to `192m` and lower heap cap from `MaxRAMPercentage=52` to `42`, then redeploy to revision `c1ccd42`; after rollout, backend came back with `RestartCount=0` and the new `JAVA_TOOL_OPTIONS` visible in logs.
- 2026-05-07: on the 2 GB VPS, the next safe production rebalance after the metaspace fix was `backend 768m`, `minio 192m`, `web 64m`; backend was the only core service consistently close to its limit, while web and MinIO had clear headroom.
- 2026-05-07: production SSH was moved off `root + password`; key login as `deploy` worked, `/opt/messenger-app` was chowned to `deploy`, and password-based root SSH was confirmed blocked after applying `/etc/ssh/sshd_config.d/99-messenger-hardening.conf`.
- 2026-05-07: the auth flow around `verification -> login` is smoother if login accepts either username or email; current implementation now keeps the existing `username` request field but resolves `email` identifiers server-side in `AuthService.login(...)`.
- 2026-05-07: targeted auth verification stayed green with `backend: mvn -q "-Dtest=AuthServiceTest,EmailVerificationServiceTest" test` and `web: npm run test:run -- AuthCard App`.
- 2026-05-07: GitHub button deploy can reuse the same SSH rollout path as the local Windows button by wrapping `deploy/remote-update.sh` in a small portable helper (`deploy/deploy-via-ssh.sh`) and feeding SSH key / known_hosts through GitHub secrets.
- 2026-05-07: GitHub SSH deploy hit a real server-side lock-file regression because earlier root-driven rollouts left `/tmp/messenger-remote-update.lock` owned by `root`; `deploy/remote-update.sh` now keeps its host-level lock under `$APP_DIR/.deploy/remote-update.lock`, which is writable by the `deploy` checkout owner.
- 2026-05-08: when the user reported the server as "down", the production stack was actually up: `backend/web/edge/postgres/redis/minio/jitsi*` all had `RestartCount=0`, public `/` and `/meet/` returned `200`, and `build-meta.json` served revision `b5706c8`.
- 2026-05-08: notable production anomalies during the server check were `backend` memory around `616 MiB / 768 MiB`, `jitsi-jicofo` near limit at `90.75 MiB / 96 MiB`, repeated websocket abnormal closes (`1001/1006`, `EOFException`) in backend logs, and Redis warning that `vm.overcommit_memory=1` is not enabled on the host.
- 2026-05-08: leftover exited anonymous helper containers (`exit 28`) from old MinIO download attempts were still present in `docker ps -a`; they are not part of the running stack but add noise during incident triage.
- 2026-05-08: `deploy` has no passwordless `sudo`, but because it is in the `docker` group, a temporary privileged container was sufficient to fix the host Redis prerequisite and persist `vm.overcommit_memory = 1` into `/etc/sysctl.d/99-messenger-redis.conf`.
- 2026-05-08: after the Redis host fix, the old exited helper containers were removed and the public site still answered `200`; the running production compose stack stayed healthy throughout the cleanup.
- 2026-05-08: the next production memory rebalance after the Redis cleanup is `jitsi-jicofo 128m`, `minio 160m`, `web 48m`; `jicofo` was the only Jitsi control-plane service critically close to its limit, while `web` and `minio` still had headroom.
- 2026-05-08: websocket `1001/1006` and `EOFException` disconnects are partly explainable by client lifecycle handling: `realtime.ts` already had hidden-tab pause/resume helpers, but they were not wired to `visibilitychange`, so background tabs relied on browser/network teardown instead of proactive pause/reconnect.
- 2026-05-08: the lifecycle websocket fix stayed green with `web: npm run test:run -- realtime`.
- 2026-05-08: production rollout to revision `648f561` succeeded through `deploy/remote-update.sh`; public `build-meta.json` updated, `vm.overcommit_memory` stayed at `1`, and sampled runtime usage after rollout was roughly backend `587 MiB / 768 MiB`, web `6 MiB / 48 MiB`, MinIO `73 MiB / 160 MiB`, Jitsi Jicofo `114 MiB / 128 MiB`, JVB `126 MiB / 192 MiB`, edge `14 MiB / 64 MiB`.
- 2026-05-08: immediately after the `648f561` rollout, backend websocket `EOFException` / `1006` entries were visible only at the rollout reconnect timestamp (`2026-05-08T07:24:44Z`); no later `EOFException`, `1001/1006`, or slow-send warnings appeared in the next sampled log window.
- 2026-05-08: the repo previously had no OpenAPI/Swagger layer; adding tester-facing API docs required all three pieces together: `springdoc-openapi-starter-webmvc-ui`, `SecurityConfig` allow-list entries for docs paths, and `deploy/Caddyfile` proxy routes for `/v3/api-docs*` and `/swagger-ui*`, otherwise docs would work only on direct backend port and 404 behind the production edge.
- 2026-05-08: verification after the OpenAPI addition succeeded with `backend: mvn -q test` and `web: npm run typecheck`, `npm run test:run`, `npm run build`.
- 2026-05-08: chat-bound conference support exposed a subtle Java trap: `Map.of(...).getOrDefault(null, ...)` throws `NullPointerException`, so non-chat conferences need explicit `chatId == null` branches when resolving chat memberships.
- 2026-05-08: verification after the group-call integration succeeded with `backend: mvn -q test`, `web: npm run typecheck`, `npm run test:run`, and `npm run build`.
- 2026-05-08: the most visible remaining mojibake was in `ChatListPanel.tsx` and `useMessageActions.ts`; after normalizing those strings and deleting dead code in the message delete wrappers, `web: npm run typecheck`, `npm run test:run -- useMessageActions chatWorkspaceUtils ActiveChatConversation.composer`, and `npm run build` stayed green.
- 2026-05-08: Telegram-style "media and files" browsing cannot be implemented honestly from the currently loaded message slice only; it needs a server-paged attachment query per chat, otherwise older chat files disappear as soon as message history pagination/windowing changes.
- 2026-05-08: verification after adding the chat attachment browser succeeded with `backend: mvn -q -Dtest=ChatAttachmentBrowserServiceTest test`, `backend: mvn -q test`, `web: npm run typecheck`, `web: npm run test:run`, `web: npm run test:run -- api`, and `web: npm run build`.
- 2026-05-08: jumping from that browser to an old source message can be bootstrapped from the existing message page API if each attachment item also carries `messageServerOrder`; the web client can seed its first `messages/page` cursor with `serverOrder + 1` and let normal older-page pagination continue from there.
- 2026-05-08: the current source-message jump is intentionally V1: it opens the chat on a slice ending at the target message, not a richer "around message with newer context" view. Verification after the jump work succeeded with `backend: mvn -q -Dtest=ChatAttachmentBrowserServiceTest test`, `backend: mvn -q test`, `web: npm run typecheck`, `web: npm run test:run -- ChatAttachmentBrowserSheet useMessageStreamNavigation api`, and `web: npm run build`.
- 2026-05-08: the attachment-browser photo viewer could safely stay frontend-only; the only runtime bug found during implementation was that slow preview blobs could time out into a permanent error state unless the shared preview hook explicitly reset `previewError` on eventual success. Verification after the viewer overlay work succeeded with `web: npm run typecheck`, `web: npm run test:run -- ChatAttachmentBrowserSheet`, and `web: npm run build`.
- 2026-05-08: because message bodies are encrypted at rest, a Telegram-style shared-links browser cannot be implemented honestly with SQL over `chat_messages`; the cheap production-safe path is to extract HTTP(S) URLs during message send/edit and persist them separately. Verification after the link-browser work succeeded with `backend: mvn -q "-Dtest=MessageLinkExtractorTest,ChatLinkBrowserServiceTest" test`, `backend: mvn -q test`, `web: npm run typecheck`, `web: npm run test:run -- api ChatAttachmentBrowserSheet`, and `web: npm run build`.
- 2026-05-08: production logs over a 24h window stayed restart-free (`RestartCount=0`, `OOMKilled=false` for backend/edge/redis/jitsi), but they did show three real anomaly classes: repeated websocket peer disconnects (`EOFException`, `1006`), a backend `ConstraintViolationException` on `uk_user_pending_outgoing_messages_user_client`, and a Jitsi bridge allocation failure (`ColibriAllocationFailedException: Timeout`, followed by `There are no operational bridges`).
- 2026-05-08: the backend duplicate-key failure happened while persisting `user_pending_outgoing_messages`; the failing unique pair was `(user_id, client_message_id)`, which means failed-send persistence is still racy/idempotency-unsafe under retries for the same client message id.
- 2026-05-08: edge logs also showed common internet scanners probing `/.env*` and `phpinfo.php`; they currently get `200` with the SPA fallback HTML rather than a hard `404`, so this is not a secret leak but it does weaken observability and should be tightened.
- 2026-05-08: the pending-outgoing race was fixed without a native SQL upsert: `PendingOutgoingMessageService.upsertOwnPendingOutgoingMessage(...)` now locks the authenticated `UserAccount` row (`findByIdForUpdate`) before lookup/save, and targeted verification passed with `backend: mvn -q "-Dtest=PendingOutgoingMessageServiceTest,MessageServiceTest" test` plus full `backend: mvn -q test`.
- 2026-05-08: the Jitsi bridge timeout correlated with JVB-side IQ processing delays (`Took 12011 ms to process an IQ`, `ProbeController ... timeout`) rather than with container restarts or OOM; the first safe mitigation in repo config is more CPU headroom for `jitsi-jvb` plus explicit websocket domain/server-id advertisement.
- 2026-05-08: `deploy/Caddyfile` had no deny rule for junk probe paths, so scanners hitting `/.env*` and `phpinfo.php` were falling through to the SPA and getting `200`. The proxy layer now owns those responses with explicit `404`s.
- 2026-05-08: production preflight still passed after the edge/Jitsi config changes (`bash deploy/preflight-prod.sh .env.prod.example`), but local Docker Compose rendering could not be re-verified in this workstation session because `docker` is not installed in the shell environment.
- 2026-05-08: production rollout to revision `eecb6d8` succeeded cleanly: `backend`, `edge`, and `jitsi-jvb` restarted onto the new revision with `RestartCount=0`, public `build-meta.json` updated, and direct checks confirmed `/.env` and `/phpinfo.php` now return `404`.
- 2026-05-08: in the first post-deploy log window there were no fresh backend duplicate-key errors and no new Jitsi `ColibriAllocationFailedException` events; only startup-time bridge churn (`Lost a bridge`) and normal client websocket peer disconnects (`EOFException`, `1006`) appeared.
- 2026-05-08: repeated production `DELETE /api/chats/{chatId}/draft -> 404` noise was avoidable client-state cleanup, not a real error; `ChatDraftService.deleteOwnDraft(...)` now deletes by `(userId, chatId)` without a chat-visibility check, and verification passed with `backend: mvn -q "-Dtest=ChatDraftServiceTest" test` plus full `backend: mvn -q test`.
- 2026-05-08: a later log check showed production was still on revision `eecb6d8`, not the newer `84829b0` draft-delete fix, so `DELETE /api/chats/{chatId}/draft -> 404` remains expected noise until the next deploy.
- 2026-05-08: the first `edge` probe hardening only covered root-style paths; nested probe variants like `/portal/phpinfo.php` and `/phpinfo/info.php` still fell through to the SPA and returned `200`.
- 2026-05-08: `deploy/Caddyfile` deny matching was then broadened to nested `/.env*`, `/.git*`, and `phpinfo*` path segments; local regex checks matched probe samples such as `/portal/phpinfo.php` and `/foo/.env` while leaving `/meet/config.js` untouched, and `bash deploy/preflight-prod.sh .env.prod.example` still passed.
- 2026-05-09: adding mailbox endpoints to `AuthController` also changed the constructor contract for `SecurityConfigTest`; the web-slice test now needs a mocked `UserMailboxService` or Spring fails the controller context before any security assertions run.
- 2026-05-09: the new mail-toggle block in `ProfileSettingsCard.tsx` initially landed with mojibake Cyrillic while adjacent legacy strings were valid UTF-8; when patching Russian UI copy from this shell, verify the saved file content itself, not only terminal rendering.
- 2026-05-09: `UpdateProfileRequest.mailEnabled` cannot stay a primitive `boolean` without silently disabling the Mail tab for older clients that omit the new field; the request/service contract now preserves the stored flag when `mailEnabled` is absent.
- 2026-05-09: verification after the optional mailbox/Mail-tab work succeeded with `backend: mvn -q "-Dtest=SecurityConfigTest,UserMailboxServiceTest" test`, `backend: mvn -q test`, `web: npm run test:run -- useWorkspaceStatus ProfileSettingsCard SidebarManagementSheets`, `web: npm run test:run`, `web: npm run typecheck`, and `web: npm run build`.
- 2026-05-09: the remaining visible workspace mojibake was in `web/src/features/chat/components/WorkspaceSidebar.tsx` (menu/search/tab copy) and `WorkspaceConversation.tsx` (loading/empty-state copy); replacing those literals with ASCII-safe `\u` escapes fixed the broken UI text without changing workspace behavior.
- 2026-05-09: on this Windows shell, direct `npm ...` calls from PowerShell fail on the disabled `npm.ps1` execution policy; `cmd /c npm run typecheck` and `cmd /c npm run build` worked for frontend verification.
- 2026-05-09: new-group auto-open behavior came from a dedicated follow-up path in `web/src/features/chat/hooks/useWorkspaceMutations.ts` (`onGroupCreated(chat, { openMenu })`) plus `NorthMessengerWorkspace.tsx` state/effect (`pendingGroupMenuOpenChatId -> setIsChatMenuOpen(true)`); removing that contract keeps post-create navigation into the new group but no longer forces the group menu open.
- 2026-05-09: verification for the group-create follow-up fix succeeded with `web: cmd /c npm run test:run -- useWorkspaceMutations` and `web: cmd /c npm run typecheck`.
- 2026-05-09: production logs on server revision `b0ee13c` showed the stack healthy (`backend/web/postgres/redis` healthy; `edge`/Jitsi/MinIO up) but repeated backend `42P18` failures in the chat media browsers: 31 hits on `GET /api/chats/{id}/attachments/browser` and 16 hits on `GET /api/chats/{id}/links/browser` over the last 24h, all failing with `ERROR: could not determine data type of parameter $2`.
- 2026-05-09: those prod browser failures line up with JPQL in `backend/src/main/java/com/north/messenger/domain/repository/ChatAttachmentRepository.java` and `ChatMessageLinkRepository.java`; both queries use nullable predicates like `(:visibleFrom is null or message.createdAt >= :visibleFrom)`, which PostgreSQL/Hibernate is currently failing to type in production.
- 2026-05-09: from this Windows workstation, SSH works via the normal `%USERPROFILE%\\.ssh` alias `pishi.ktsf.ru`; using the mirrored OneDrive desktop key path directly hit OpenSSH hostfile parsing and `UNPROTECTED PRIVATE KEY FILE` issues.
- 2026-05-09: the production media-browser `42P18` fix was to remove JPQL `:param is null` checks from `ChatAttachmentRepository.findBrowserItems(...)` and `ChatMessageLinkRepository.findBrowserItems(...)`; the services now pass explicit `applyVisibleFrom` / `applyCursor` booleans plus typed fallback values so PostgreSQL never sees an untyped null placeholder in those queries.
- 2026-05-09: verification after the media-browser query fix succeeded with `backend: mvn -q "-Dtest=ChatAttachmentBrowserServiceTest,ChatLinkBrowserServiceTest" test` and full `backend: mvn -q test`.
- 2026-05-09: auth form validation is now centralized in `web/src/features/auth/authValidation.ts`; registration blur/submit errors, disabled submit states, and the profile password-change form all share the same frontend checks that mirror backend `PasswordPolicyService`.
- 2026-05-09: concurrent typing could wipe the focused composer because `ActiveChatConversation.next.tsx` replayed stale `activeDraft` values back into the textarea during sync; skipping that write while the textarea is focused preserves the local draft.
- 2026-05-09: account deletion was erasing some direct-chat history indirectly (`deleteDirectRoomsWithFewerThanTwoParticipants`) and several renderers skipped unknown senders; preserving orphaned direct rooms and substituting `Deleted user` placeholders in chat/message/link/attachment paths keeps history visible after peer deletion.
- 2026-05-09: conference invite-link refresh from the UI was reusing the existing code because `NorthMessengerWorkspace.tsx` called `createConferenceInviteLink` without `refresh=1`; once a code exists, the client now sends the refresh flag explicitly.
- 2026-05-09: editing a message that still contains links could hit the unique key `uk_chat_message_links_message_position` because `ChatMessageLinkService.syncLinks(...)` deleted and reinserted rows in one transaction without flushing the delete first.
- 2026-05-09: `EmailVerificationEmailNotifier` was swallowing mail-sender failures in its `AFTER_COMMIT` listener, so `/api/auth/*email-verification*` could still return `202` while delivery only failed in logs; the listener now lets delivery exceptions propagate.
- 2026-05-09: chat-list failed-send state is now surfaced from `pendingOutgoingMessages` with a dedicated list indicator, profile name/about fields show backend-aligned `40/160` character counters with long-name wrapping, user search ordering is exact > prefix > partial in `UserAccountRepository.searchByUsernameOrDisplayName(...)`, and search dropdowns close on blur unless focus moves onto a result action.
- 2026-05-09: verification after the QA bugfix batch succeeded with `web: cmd /c npm run test:run`, `web: cmd /c npm run typecheck`, `web: cmd /c npm run build`, `backend: mvn -q "-Dtest=InviteLinkServiceTest,EmailVerificationEmailNotifierTest,ChatMessageLinkServiceTest,UserAccountRepositoryTest" test`, and full `backend: mvn -q test`.
- 2026-05-09: group-ban management previously had only `POST /api/chats/{chatId}/bans`; fixing the re-add-after-unban QA bug required a real unban/read path (`GET` + `DELETE /api/chats/{chatId}/bans...`) plus a frontend banned-users section in the group members UI so removing the `chat_room_bans` row is actually reachable from the product.
- 2026-05-09: failed direct messages could visually collide with the bottom-right timestamp/status block because direct-chat meta is absolutely positioned; when `message.status.state === FAILED`, the bottom meta now switches to normal flow (`.is-inline`) and the failure row can wrap, preventing retry/timestamp overlap.
- 2026-05-09: verification after the unban/retry-layout follow-up succeeded with `backend: mvn -q "-Dtest=ChatServiceTest" test`, `web: cmd /c npm run test:run -- ActiveChatConversation.composer useWorkspaceMutations api SidebarManagementSheets`, `web: cmd /c npm run typecheck`, then full `web: cmd /c npm run test:run`, `web: cmd /c npm run build`, and `backend: mvn -q test`.
- 2026-05-09: local attachment upload/download paths depend on `APP_MEDIA_MESSAGE_ATTACHMENTS_MINIO_PUBLIC_ENDPOINT=http://localhost:5173/storage`; Vite must strip the `/storage` prefix before proxying to MinIO or presigned message-file URLs fail with MinIO `403 AccessDenied`, which breaks both direct-chat and group-chat file attachments on localhost.
- 2026-05-09: `web/public/push-sw.js` was suppressing all push notifications whenever any North Messenger tab existed, even a hidden background tab; only visible/focused same-origin clients should suppress the generic service-worker notification.
- 2026-05-09: backend password change logic already revokes sessions and updates the hash correctly; the QA regression was covered by adding `AuthServiceTest.changePasswordShouldRequireNewPasswordForNextLogin`, which proves old credentials fail and the new password can log in immediately after the change.
- 2026-05-09: verification after the attachment/push/password follow-up succeeded with `web: cmd /c npm run test:run -- pushServiceWorker viteConfig pushNotifications ProfileSettingsCard`, `backend: mvn -q "-Dtest=AuthServiceTest" test`, `web: cmd /c npm run typecheck`, `web: cmd /c npm run test:run`, `web: cmd /c npm run build`, and `backend: mvn -q test`.
- 2026-05-09: draft-only direct chats were still filtered out in `NorthMessengerWorkspace.tsx` by `lastMessageAt !== null`; the safe fix was to track per-chat draft activity in `useChatDrafts.ts`, keep those chats listable in `useWorkspaceStatus.ts`, and sort idle chat order by `max(chat.updatedAt, draft.updatedAt)` when no search is active.
- 2026-05-09: the visible edited-time bug came from `ActiveChatConversation.next.tsx` labeling a message as edited but still formatting `createdAt`; the timeline meta now uses `editedAt ?? createdAt`, and the desktop/mobile workspace breakpoint was lowered from `960px` to `820px` so the conversation pane no longer disappears around half-width desktop windows.
- 2026-05-09: chat-list reaction indication now rides on the existing chat-summary websocket path instead of a new client-only side channel: backend `ChatSummaryResponse` gained `lastMessageHasReactions`, `MessageRealtimeEventListener` now also calls `chatService.notifyChatUpdated(...)` after reaction broadcasts, and `ChatListPanel.tsx` shows a small reaction pill when the latest visible message has reactions.
- 2026-05-09: `ChatListPanel.tsx` had to be fully rewritten in ASCII-safe form because JSX attributes like `title="\u0415..."` keep the backslash escapes literally; to emit real Cyrillic in DOM attributes, use expression form `title={"\u0415..."}`.
- 2026-05-09: while adding draft-activity tracking, `useChatDrafts.ts` regressed because the unload effect depended on an Effect Event and re-flushed drafts on ordinary rerenders; the fix was to keep the `pagehide/beforeunload` effect non-reactive (`[]`) and to route empty composer changes through `syncDraftLocally(chatId, "")` so cleared drafts disappear immediately from local/query state.
- 2026-05-09: verification after the chat-list/chat-layout follow-up succeeded with `web: cmd /c npm run typecheck`, `web: cmd /c npm run test:run -- useChatDrafts ChatListPanel chatState useWorkspaceStatus ActiveChatConversation.next`, full `web: cmd /c npm run test:run`, `web: cmd /c npm run build`, `backend: mvn -q "-Dtest=ChatServiceTest,ChatOpenServiceTest,WorkspaceSearchServiceTest,MessageRealtimeEventListenerTest" test`, and full `backend: mvn -q test`.
- 2026-05-09: direct-chat `delete for self` was still effectively an unhide because `createDirectChat(...)` and `MessageCommandService.sendMessage(...)` cleared `user_deleted_chats` without changing direct-history visibility; the safe fix was to reopen only for the acting user, move that direct membership's `joinedAt` forward to the user's `deletedAt`, and then reuse `joinedAt` as the direct-chat visible-history cutoff in summary/message/link/attachment/reply paths.
- 2026-05-09: verification after the direct-chat reopen/history-cutoff fix succeeded with `backend: mvn -q "-Dtest=ChatServiceTest,MessageServiceTest,ChatAttachmentBrowserServiceTest,ChatLinkBrowserServiceTest" test` and full `backend: mvn -q test`.
- 2026-05-09: forward-message UX already had working mutation/error plumbing, but the dedicated forward sheet showed no local state; `SidebarUtilitySheets.tsx` now renders an inline pending status and inline forwarding error so the user sees progress/failure where the action started, while successful forward still resolves by opening the target chat.
- 2026-05-09: verification after the forward-sheet status fix succeeded with `web: cmd /c npm run test:run -- SidebarUtilitySheets`, `web: cmd /c npm run typecheck`, and `web: cmd /c npm run build`.
- 2026-05-09: pinned messages are no longer modeled as a single `chat_rooms.pinned_message_id`; `V5__add_chat_pinned_messages.sql` introduces `chat_pinned_messages`, `ChatService.updatePinnedMessage(...)` now toggles one message within that collection, and the legacy room-level pinned fields are kept only as a compatibility alias pointing at the newest remaining pin.
- 2026-05-09: the pinned banner now supports multiple messages end-to-end: backend `ChatSummaryResponse` exposes `pinnedMessages`, snippets carry `serverOrder`, `NorthMessengerWorkspace.tsx` tracks the active pinned index/id, and mobile `Перейти` reuses the seeded message-jump path so older pinned messages can be fetched and scrolled into view instead of only working for already loaded history.
- 2026-05-09: verification after the multi-pin/mobile pinned follow-up succeeded with `web: cmd /c npm run typecheck`, full `web: cmd /c npm run test:run`, `web: cmd /c npm run build`, `backend: mvn -q "-Dtest=ChatServiceTest,MessageServiceTest,ChatOpenServiceTest,WorkspaceSearchServiceTest" test`, and full `backend: mvn -q test`.
- 2026-05-09: production `PROF-006` verification is no longer only inferred from code: `/opt/messenger-app/.env.prod` has email verification enabled against `smtp.timeweb.ru:465`, SMTP login and `RCPT TO` both succeeded for `noreply@ktsf.ru` and a plus-address alias, and a live temp-account flow on `https://pishi.ktsf.ru` (`register -> POST /api/auth/me/email-verification -> delete account`) returned `200/202/204`, rotated verification tokens from `1|1` to `2|1`, and produced no mail-related backend exceptions since the test start.
- 2026-05-10: production is already on revision `3880966` (`Fix QA issues across auth chat groups and profile`, built at `2026-05-09T20:16:22.550Z`), so fresh log anomalies after that time are post-deploy, not stale pre-fix noise.
- 2026-05-10: a 24h prod log sweep after deploy showed no backend `ERROR` lines, but there is still a live attachment-upload failure outside backend: Caddy/MinIO logged `12` repeated `403 PUT /storage/message-attachments/...` requests between `2026-05-10T05:12:30Z` and `2026-05-10T15:42:15Z`; the requests reached MinIO through presigned URLs with `X-Amz-SignedHeaders=content-type;host` and browser `Content-Type: image/jpeg`, so the remaining bug is on the presigned upload path, not the chat-message API.
- 2026-05-10: other prod anomalies in the same 24h window were lower-severity: `16` edge `status=0` client-aborted requests (mostly `GET /api/chats` plus a couple `/api/auth/refresh` and typing polls), `10` ordinary `401 POST /api/auth/refresh` responses, a single probe `404 GET /.git/config`, backend startup warning about ephemeral VAPID keys (`APP_PUSH_VAPID_*` unset, existing subscriptions will need refresh after restart), and a handful of Jitsi disconnect warnings (`recv() failed` / `Participant no longer active`) that look like short call/session churn rather than a persistent outage.
