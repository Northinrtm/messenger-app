jicofo {
  conference {
    // Keep conference alive for 5 minutes after only one participant remains,
    // so they are not kicked immediately when the other party disconnects.
    single-participant-timeout = 5 minutes
  }
}
